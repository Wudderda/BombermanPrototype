using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Enumeration to identify objects to be pooled.
/// </summary>
public enum ObjectPoolEnum 
{
    BRICK = 50,
    ENEMY = 15,
    BOMB = 7,
    EXPLOSION_EFFECT = 45,
    BOMB_RANGE_POWERUP = 4,
    BOMB_COUNT_POWERUP = 5,
    DOOR = 1
}
